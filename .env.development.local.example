# Development Local Override (for testing local app against production API)
#
# To use this file:
# 1. Copy this file: cp .env.development.local.example .env.development.local
# 2. This will override .env.development settings
# 3. The .local file is gitignored, so it won't be committed
#
# Use this when you want to run the local app (npm run dev) against production API

VITE_API_URL=https://api.plixo.com
